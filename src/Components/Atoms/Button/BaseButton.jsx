import styled from "styled-components";

export const BaseButton = styled.button`
  color: white;
  padding: 12px 30px;
  border: none;
  outline: none;
  border-radius: 9999px;

  &:hover {
    cursor: pointer;
    opacity: .8;
  }
`
